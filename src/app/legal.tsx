import { useLocalSearchParams } from 'expo-router';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { colors, radius, spacing, typography } from '@/theme/tokens';
const terms = ['Use of Service', 'Parking Reservations', 'Payments', 'Cancellations & Refunds', 'Parking Facility Responsibility', 'User Responsibilities', 'Accounts', 'Changes to Terms']; const privacy = ['Information We Collect', 'Location Information', 'Booking Information', 'Payment Information', 'How Information Is Used', 'Data Storage', 'Notifications', 'User Choices', 'Data Deletion', 'Contact'];
export default function LegalScreen() { const { type } = useLocalSearchParams<{ type: string }>(); const isPrivacy = type === 'privacy'; const sections = isPrivacy ? privacy : terms; return <SafeAreaView style={styles.screen}><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>{isPrivacy ? 'Privacy Policy' : 'Terms & Conditions'}</Text><View style={styles.note}><Text style={styles.noteText}>Development placeholder copy — not final legal documentation.</Text></View>{sections.map((title, index) => <View key={title} style={styles.section}><Text style={styles.number}>{String(index + 1).padStart(2, '0')}</Text><View style={styles.copy}><Text style={styles.heading}>{title}</Text><Text style={styles.body}>ParkEase currently stores mock account, booking and preference data locally on this device to provide this development experience.</Text></View></View>)}</ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.background }, content: { padding: spacing.lg, gap: spacing.lg, paddingBottom: spacing.xxxl }, title: { ...typography.title1, color: colors.textPrimary }, note: { padding: spacing.md, borderRadius: radius.md, backgroundColor: colors.primarySoft }, noteText: { ...typography.bodySmall, color: colors.textSecondary, lineHeight: 20 }, section: { flexDirection: 'row', gap: spacing.md, paddingBottom: spacing.lg, borderBottomWidth: 1, borderBottomColor: colors.borderSubtle }, number: { ...typography.captionStrong, color: colors.primary, marginTop: 3 }, copy: { flex: 1 }, heading: { ...typography.title3, color: colors.textPrimary }, body: { ...typography.bodySmall, color: colors.textSecondary, lineHeight: 21, marginTop: spacing.xs } });
