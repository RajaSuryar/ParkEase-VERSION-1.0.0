import { MapCoordinate } from '@/types/domain';

// Chennai is a development fallback only; live coordinates replace it when permission is granted.
export const fallbackMapCoordinate: MapCoordinate = { latitude: 13.0827, longitude: 80.2707 };
export const fallbackLocationLabel = 'Choose your location';
