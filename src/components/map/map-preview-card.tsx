import { Ionicons } from '@expo/vector-icons';
import { Href, router } from 'expo-router';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, radius, shadows, spacing, typography } from '@/theme/tokens';

export function MapPreviewCard() { return <Pressable accessibilityRole="button" accessibilityLabel="Explore parking on map" onPress={() => router.push('/map' as Href)} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.lineOne} /><View style={styles.lineTwo} /><View style={styles.marker}><Ionicons name="car" size={14} color={colors.textOnPrimary} /></View><View style={styles.copy}><Text style={styles.title}>Explore parking on map</Text><Text style={styles.subtitle}>See availability around you</Text></View><View style={styles.action}><Ionicons name="arrow-forward" size={18} color={colors.primary} /></View></Pressable>; }
const styles = StyleSheet.create({ card: { height: 132, overflow: 'hidden', justifyContent: 'flex-end', padding: spacing.lg, borderRadius: radius.xl, backgroundColor: colors.mapBackground, ...shadows.card }, pressed: { opacity: 0.82 }, lineOne: { position: 'absolute', width: 300, height: 36, top: 40, left: -32, transform: [{ rotate: '-18deg' }], backgroundColor: colors.surface }, lineTwo: { position: 'absolute', width: 250, height: 24, top: 76, right: -38, transform: [{ rotate: '22deg' }], backgroundColor: '#D6E1E5' }, marker: { position: 'absolute', top: 28, right: 70, width: 34, height: 34, alignItems: 'center', justifyContent: 'center', borderRadius: radius.full, backgroundColor: colors.primary, borderWidth: 3, borderColor: colors.surface }, copy: { zIndex: 1 }, title: { ...typography.title3, color: colors.textPrimary }, subtitle: { ...typography.bodySmall, color: colors.textSecondary, marginTop: 2 }, action: { position: 'absolute', right: spacing.lg, bottom: spacing.lg, width: 38, height: 38, alignItems: 'center', justifyContent: 'center', borderRadius: radius.full, backgroundColor: colors.surface } });
