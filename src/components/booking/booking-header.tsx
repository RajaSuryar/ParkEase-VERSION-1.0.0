import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, radius, spacing, typography } from '@/theme/tokens';

export function BookingHeader({ title, subtitle, step }: { title: string; subtitle: string; step: 1 | 2 | 3 }) { return <View style={styles.wrap}><View style={styles.top}><Pressable accessibilityRole="button" accessibilityLabel="Back" onPress={() => router.back()} style={styles.back}><Ionicons name="arrow-back" size={20} color={colors.textPrimary} /></Pressable><Text style={styles.step}>STEP {step} OF 3</Text></View><Text style={styles.title}>{title}</Text><Text style={styles.subtitle}>{subtitle}</Text><View style={styles.progress}>{[1, 2, 3].map((value) => <View key={value} style={[styles.progressItem, value <= step && styles.progressActive]} />)}</View></View>; }
const styles = StyleSheet.create({ wrap: { paddingHorizontal: spacing.lg, paddingTop: spacing.md }, top: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, back: { width: 42, height: 42, alignItems: 'center', justifyContent: 'center', borderWidth: 1, borderColor: colors.borderSubtle, borderRadius: radius.full, backgroundColor: colors.surface }, step: { ...typography.captionStrong, color: colors.primary, letterSpacing: 0.8 }, title: { ...typography.title1, color: colors.textPrimary, marginTop: spacing.lg }, subtitle: { ...typography.bodySmall, color: colors.textSecondary, marginTop: spacing.xs }, progress: { flexDirection: 'row', gap: spacing.sm, marginTop: spacing.lg }, progressItem: { flex: 1, height: 4, borderRadius: radius.full, backgroundColor: colors.border }, progressActive: { backgroundColor: colors.primary } });
