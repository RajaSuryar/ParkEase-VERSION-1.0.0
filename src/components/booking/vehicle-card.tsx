import { Ionicons } from '@expo/vector-icons';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Vehicle } from '@/types/domain';
import { colors, radius, shadows, spacing, typography } from '@/theme/tokens';

const icons: Record<Vehicle['type'], keyof typeof Ionicons.glyphMap> = { car: 'car-outline', suv: 'car-sport-outline', motorcycle: 'bicycle-outline', bike: 'bicycle-outline' };
export function VehicleCard({ vehicle, selected, onPress, onEdit }: { vehicle: Vehicle; selected?: boolean; onPress?: () => void; onEdit?: () => void }) { return <Pressable accessibilityRole="radio" accessibilityState={{ selected }} accessibilityLabel={`${vehicle.registrationNumber}, ${selected ? 'selected' : 'available vehicle'}`} onPress={onPress} style={[styles.card, selected && styles.selected]}><View style={styles.icon}><Ionicons name={icons[vehicle.type]} size={25} color={colors.primary} /></View><View style={styles.copy}><View style={styles.registrationRow}><Text style={styles.registration}>{vehicle.registrationNumber}</Text>{vehicle.isDefault ? <Text style={styles.badge}>DEFAULT</Text> : null}</View><Text style={styles.model}>{vehicle.brand} {vehicle.model}</Text><Text style={styles.color}>{vehicle.color} · {vehicle.type}</Text></View>{onEdit ? <Pressable accessibilityRole="button" accessibilityLabel={`Edit ${vehicle.registrationNumber}`} onPress={(event) => { event.stopPropagation(); onEdit(); }} hitSlop={spacing.sm} style={styles.edit}><Ionicons name="create-outline" size={19} color={colors.primary} /></Pressable> : <Ionicons name={selected ? 'checkmark-circle' : 'radio-button-off'} size={23} color={selected ? colors.primary : colors.textTertiary} />}</Pressable>; }
const styles = StyleSheet.create({ card: { minHeight: 92, flexDirection: 'row', alignItems: 'center', gap: spacing.md, padding: spacing.md, borderWidth: 1.5, borderColor: colors.border, borderRadius: radius.lg, backgroundColor: colors.surface, ...shadows.card }, selected: { borderColor: colors.primary, backgroundColor: colors.primarySoft }, icon: { width: 48, height: 48, alignItems: 'center', justifyContent: 'center', borderRadius: radius.md, backgroundColor: colors.primarySoft }, copy: { flex: 1 }, registrationRow: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm }, registration: { ...typography.bodyStrong, color: colors.textPrimary }, badge: { ...typography.captionStrong, paddingHorizontal: 6, paddingVertical: 2, borderRadius: radius.full, color: colors.primary, backgroundColor: colors.primarySoft }, model: { ...typography.bodySmall, color: colors.textPrimary, marginTop: 2 }, color: { ...typography.caption, color: colors.textSecondary, marginTop: 2, textTransform: 'capitalize' }, edit: { width: 36, height: 36, alignItems: 'center', justifyContent: 'center', borderRadius: radius.full, backgroundColor: colors.primarySoft } });
