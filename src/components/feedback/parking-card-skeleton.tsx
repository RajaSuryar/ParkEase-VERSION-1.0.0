import { StyleSheet, View } from 'react-native';
import { colors, radius, spacing } from '@/theme/tokens';
export function ParkingCardSkeleton({ variant = 'compact' }: { variant?: 'compact' | 'list' }) { return <View style={[styles.card, variant === 'list' && styles.listCard]}><View style={styles.thumbnail} /><View style={styles.content}><View style={styles.lineWide} /><View style={styles.lineMedium} /><View style={styles.lineSmall} /></View></View>; }
const styles = StyleSheet.create({ card: { width: 292, minHeight: 154, padding: spacing.md, flexDirection: 'row', gap: spacing.md, borderRadius: radius.lg, backgroundColor: colors.surface }, listCard: { width: '100%' }, thumbnail: { width: 90, borderRadius: radius.md, backgroundColor: colors.surfaceMuted }, content: { flex: 1, justifyContent: 'space-around' }, lineWide: { width: '90%', height: 16, borderRadius: radius.full, backgroundColor: colors.surfaceMuted }, lineMedium: { width: '68%', height: 12, borderRadius: radius.full, backgroundColor: colors.surfaceMuted }, lineSmall: { width: '46%', height: 12, borderRadius: radius.full, backgroundColor: colors.surfaceMuted } });
