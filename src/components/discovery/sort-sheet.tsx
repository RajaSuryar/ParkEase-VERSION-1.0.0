import { Ionicons } from '@expo/vector-icons';
import { Modal, Pressable, StyleSheet, Text, View } from 'react-native';
import { ParkingSortOption } from '@/types/domain';
import { sortLabels } from '@/utils/parking-results';
import { colors, radius, spacing, typography } from '@/theme/tokens';

export function SortSheet({ value, onClose, onChange }: { value: ParkingSortOption; onClose: () => void; onChange: (value: ParkingSortOption) => void; }) { return <Modal transparent animationType="slide" onRequestClose={onClose}><View style={styles.scrim}><Pressable style={StyleSheet.absoluteFill} onPress={onClose} /><View style={styles.sheet}><View style={styles.handle} /><View style={styles.header}><Text style={styles.title}>Sort by</Text><Pressable accessibilityRole="button" onPress={onClose}><Ionicons name="close" size={24} color={colors.textPrimary} /></Pressable></View>{(Object.keys(sortLabels) as ParkingSortOption[]).map((option) => <Pressable key={option} accessibilityRole="radio" accessibilityState={{ selected: value === option }} onPress={() => { onChange(option); onClose(); }} style={styles.option}><Text style={styles.optionText}>{sortLabels[option]}</Text><Ionicons name={value === option ? 'radio-button-on' : 'radio-button-off'} size={21} color={value === option ? colors.primary : colors.textTertiary} /></Pressable>)}</View></View></Modal>; }
const styles = StyleSheet.create({ scrim: { flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(17,24,39,0.38)' }, sheet: { borderTopLeftRadius: radius.xl, borderTopRightRadius: radius.xl, paddingBottom: spacing.xl, backgroundColor: colors.surface }, handle: { alignSelf: 'center', width: 38, height: 4, borderRadius: radius.full, marginTop: spacing.sm, backgroundColor: colors.border }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', padding: spacing.lg }, title: { ...typography.title2, color: colors.textPrimary }, option: { minHeight: 56, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: spacing.lg }, optionText: { ...typography.body, color: colors.textPrimary } });
