import { Ionicons } from '@expo/vector-icons';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, radius, shadows, spacing, typography } from '@/theme/tokens';
export function SearchBar({ placeholder, onPress }: { placeholder: string; onPress?: () => void }) { return <Pressable accessibilityRole="search" accessibilityLabel={placeholder} onPress={onPress} style={({ pressed }) => [styles.container, pressed && styles.pressed]}><Ionicons name="search-outline" size={21} color={colors.textSecondary} /><Text style={styles.placeholder}>{placeholder}</Text><View style={styles.divider} /><Ionicons name="options-outline" size={20} color={colors.primary} /></Pressable>; }
const styles = StyleSheet.create({ container: { minHeight: 56, paddingHorizontal: spacing.md, flexDirection: 'row', alignItems: 'center', gap: spacing.sm, borderWidth: 1, borderColor: colors.borderSubtle, borderRadius: radius.lg, backgroundColor: colors.surface, ...shadows.card }, pressed: { backgroundColor: colors.primarySoft, opacity: 0.9 }, placeholder: { ...typography.body, color: colors.textSecondary, flex: 1 }, divider: { height: 20, width: 1, backgroundColor: colors.border } });
